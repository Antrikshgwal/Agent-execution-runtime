# Runtime dependencies. The mocks have their own list in mock/requirements.txt.
asyncpg
httpx
pydantic

# PLANNER=gemini only. The mock planner needs neither.
google-genai
python-dotenv

# linter
pylint
