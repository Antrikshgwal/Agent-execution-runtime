# Agent-execution-runtime

A durable agent execution runtime.

## Layout

- [`mock/`](mock/) — mock cloud service and Postgres schema used to demo the
  runtime. See [`mock/README.md`](mock/README.md) for setup and verification.
